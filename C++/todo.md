# C++ variant

This directory contains the imported C++ baseline implementation.

- [ ] Audit the baseline build and test workflow.
- [ ] Document the public API and compatibility requirements.
- [ ] Track fixes needed before cross-language parity work.
- [ ] Verify this variant before merging changes into `main`.
